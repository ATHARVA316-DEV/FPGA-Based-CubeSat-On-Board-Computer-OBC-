# FPGA-Based-CubeSat-On-Board-Computer-OBC-
This project builds a scaled-down satellite flight computer on an FPGA that reads sensor data, stores and packages it into telemetry, sends it to a ground-station PC, accepts commands back, and automatically switches to a safe mode if it detects a fault
